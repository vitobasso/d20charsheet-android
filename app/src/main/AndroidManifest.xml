<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vituel.dndplayer" >

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Holo.Light" >

        <activity
            android:name=".activity.summary.SummaryActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".activity.edit_char.EditCharActivity"
            android:label="@string/char_edit"/>

        <activity
            android:name=".activity.SelectCharActivity"
            android:label="@string/char_open"/>

        <activity
            android:name=".activity.SelectClassActivity"
            android:label="@string/class_select"/>

        <activity
            android:name=".activity.EditClassActivity"
            android:label="@string/class_edit"/>

        <activity
            android:name=".activity.EditItemActivity"
            android:label="@string/item_edit"/>

        <activity
            android:name=".activity.SelectItemActivity"
            android:label="@string/item_select"/>

        <activity
            android:name=".activity.SelectTempEffectActivity"
            android:label="@string/temps_select"/>

        <activity
            android:name=".activity.EditTempEffectActivity"
            android:label="@string/temps_edit"/>

        <activity
            android:name=".activity.SelectRaceActivity"
            android:label="@string/race_select"/>

        <activity
            android:name=".activity.EditRaceActivity"
            android:label="@string/race_edit"/>

        <activity
            android:name=".activity.EditFeatActivity"
            android:label="@string/trait_edit"/>

        <activity
            android:name=".activity.SelectFeatActivity"
            android:label="@string/trait_select"/>

        <activity
            android:name=".activity.EditSkillActivity"
            android:label="@string/skill_edit"/>

        <activity
            android:name=".activity.SelectSkillActivity"
            android:label="@string/skill_select"/>

        <activity
            android:name=".activity.EditAttackRoundActivity"
            android:label="@string/skill_select"/>

    </application>

</manifest>
